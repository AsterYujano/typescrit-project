{
  "name": "typescript-api",
  "version": "1.0.0",
  "description": "A simple app to learn typescript and rewrite a simple metrics collector api",
  "main": "source/server.ts",
  "scripts": {
    "build": "rm -rf build/ && prettier --write source/ && tsc",
    "dev": "nodemon source/playground/MetricsList.ts",
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.11",
    "@types/jest": "^26.0.22",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.4",
    "typescript": "^4.2.4"
  }
}
